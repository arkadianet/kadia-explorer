/target
/data
*.redb
*.duckdb

# Local UI review screenshots (regenerate with the live Playwright config)
/frontend/.uishots

# Local gate logs and fixture temporary files
/artifacts/

# Phase-1 rent candidate output (200 MB+ of JSONL; regenerate with the scanner)
/rent-candidates.jsonl
